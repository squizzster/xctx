This is a bundled file-manager demo fixture for xctx middleware testing.

It exists so the file-manager connector can adapt ordinary filesystem commands
into stable JSON objects without adding filesystem semantics to the generic
xctx runtime.
