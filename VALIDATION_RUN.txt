xctx 4.2.3 development validation
=================================

Environment used in this sandbox:
- Python: 3.12.13
- Workspace status: live local development, not a deployed public compatibility surface

Canonical validation commands:

1. YAML surface guard
   python3 .agents/skills/xctx-yaml-config/scripts/check_xctx_yaml_surface.py
   Result: PASS

2. Syntax/bytecode check
   python3 -m compileall -q bin connector_supervisor.py examples libs tests
   Result: PASS

3. Full pytest suite
   python3 -m pytest -q --durations=30
   Expected current result: 97 passed, 1 skipped

Notes:
- pytest has no default deselection configured; `pytest -q` means the full collected suite.
- `make test` runs the full pytest suite.
- `make release-test` runs the YAML checker, compileall, and the same full pytest suite.
- Fast/unit/integration Makefile targets are explicit subset/debug runs only.
- The package-install smoke remains opt-in because repeated pip/venv subprocesses are sandbox-sensitive.
- Local skills and reports are development aids; the current code, tests, and loaded YAML are the source of truth when they drift.
